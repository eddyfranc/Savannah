
services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on:
      - db

  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: sil_backend
      POSTGRES_USER: Frank
      POSTGRES_PASSWORD: Reigns3#
    ports:
      - "5432:5432"
